local Utility = require(script.Parent.Parent.Parent.Utility)
--local Interface = require(script.Parent.Parent.Interface)
local Keybind = require(script.Parent.Keybind)

return function()
    --Keybind.new("ceo", {}):Bind(function()
        -- Center object
    --end)
end